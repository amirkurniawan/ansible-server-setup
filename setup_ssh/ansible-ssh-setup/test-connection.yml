---
# Playbook: Test SSH Connectivity
# Cek koneksi ke semua server sebelum deploy
#
# Usage:
#   ansible-playbook test-connection.yml
#   ansible-playbook test-connection.yml --limit vm1

- name: Test SSH Connection to All Servers
  hosts: all_servers
  gather_facts: yes

  vars_files:
    - group_vars/all.yml

  tasks:
    - name: Ping test
      ping:
      register: ping_result

    - name: Display connection info
      debug:
        msg: |
          ✅ Connected to {{ inventory_hostname }}
          📍 Host: {{ ansible_host }}
          👤 User: {{ ansible_user }}
          🖥️  OS: {{ ansible_distribution }} {{ ansible_distribution_version }}
          🏠 Hostname: {{ ansible_hostname }}
